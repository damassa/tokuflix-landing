import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  InfoCardRight: {
    fontSize: 18,
    padding: "25px 0",
    color: "#505050",
    "& img": {
      width: "60%",
    },
  },
}));

export default useStyles;
