import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  MainContentTitle: {
    textAlign: 'center',
    padding: '50px 0',
    fontSize: 28,
    fontWeight: 'bold',
    color: '#33202a',
  },
}));

export default useStyles;
